<div class="home">
  <div class="home-image d-flex">
    <div>
      <h1 style="font-size: 50px;">Manager BOLD</h1>
      <h3 style="font-size: 40px;">For managing your objectives</h3>
      <div class="horizontal-line"></div>
      <ul>
        <li class="list-inline" style="font-size: 25px;"><i class="fas fa-file"></i>List your domains</li>
        <li class="list-inline" style="font-size: 25px;"><i class="fas fa-hourglass-half"></i>Define your objectives</li>
        <li class="list-inline" style="font-size: 25px;"><i class="fas fa-envelope-open"></i>Be rewarded</li>
      </ul>
    </div>
  </div>

  <div class="home-signup">
    <div class="form-placement">
        <h2 style="font-size:4vh;">Identify</h2>
        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          <div class="form-inputs">
            <%= f.input :email, required: false, autofocus: true, placeholder: "name@email.com", wrapper_html: { class: "offilog" } %>
            <%= f.input :password, required: false, placeholder: "••••••", wrapper_html: { class: "offilog" } %>
            <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
          </div>
          <div class="form-actions">
            <%= f.button :submit, "Log in", class: "btn btn-primary log-in" %>
          </div>
          <%= render "devise/shared/links" %>
        <% end %>
      </div>
  </div>
</div>
